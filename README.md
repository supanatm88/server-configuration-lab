# ⚙️ Server Configuration Lab (Windows Server, DHCP, File Server)

## 📘 รายละเอียดโปรเจกต์
โปรเจกต์นี้เป็นการจำลองระบบภายในองค์กร โดยใช้ **Windows Server** เพื่อฝึกการติดตั้งและตั้งค่า  
**DHCP Server** สำหรับแจก IP Address อัตโนมัติให้กับเครื่องลูกข่าย และ **File Server**  
เพื่อแชร์ไฟล์ระหว่างเครื่องในระบบ LAN  

---

## 🎯 วัตถุประสงค์
- ฝึกติดตั้งและกำหนดค่า Windows Server ให้ทำงานเป็น DHCP Server  
- ฝึกสร้างและกำหนดสิทธิ์การเข้าถึง File Server  
- เข้าใจหลักการทำงานของระบบเครือข่ายภายในองค์กร  
- จำลองการเชื่อมต่อระหว่างเครื่องลูกข่าย (Client) และเครื่องเซิร์ฟเวอร์ (Server)

---

## 🧰 สภาพแวดล้อมที่ใช้
| รายการ | รายละเอียด |
|:--|:--|
| 💿 ระบบปฏิบัติการ Server | Windows Server 2019 (with Desktop Experience) |
| 💻 ระบบปฏิบัติการ Client | Windows 10 / 11 |
| 🧱 โปรแกรมจำลอง | VMware Workstation / VirtualBox |
| 🌐 Network Type | Host-only / Internal Network |

---

## ⚙️ ขั้นตอนการติดตั้งและตั้งค่า

### 🪜 ขั้นตอนที่ 1: การตั้งค่า Windows Server
1. ติดตั้ง Windows Server และตั้งชื่อเครื่อง เช่น `SRV-DC01`
2. ตั้งค่า IP แบบ Static  

IP Address: 192.168.1.10
Subnet Mask: 255.255.255.0
Default Gateway: 192.168.1.1
DNS Server: 8.8.8.8

3. Restart เครื่องเพื่อให้การตั้งค่ามีผล

---

### 🪜 ขั้นตอนที่ 2: ติดตั้ง DHCP Server Role
1. เปิด **Server Manager** → คลิก **Add Roles and Features**
2. เลือก **Role-based Installation**
3. เลือก Role: ✅ *DHCP Server*
4. กด Next → Install  
5. เมื่อติดตั้งเสร็จ คลิก “Complete DHCP Configuration” เพื่อเปิดใช้งาน

---

### 🪜 ขั้นตอนที่ 3: ตั้งค่า DHCP Scope
1. เปิด `DHCP Management Console` (พิมพ์: `dhcpmgmt.msc`)
2. คลิกขวา IPv4 → “New Scope”
3. กำหนดค่า Scope:
```
Name: serverPool
Start IP: 192.168.1.100
End IP: 192.168.1.200
Subnet Mask: 255.255.255.0
Default Gateway: 192.168.1.1
DNS Server: 8.8.8.8
```
4. Finish ✅

📸 *ตัวอย่างการตั้งค่า DHCP Scope*  
![DHCP Config Screenshot](./screenshots/dhcp-setup.png)

---

### 🪜 ขั้นตอนที่ 4: ทดสอบ DHCP ด้วยเครื่อง Client
1. เปิด Windows 10 (Client)  
2. ตั้ง Network Adapter ให้อยู่ในวงเดียวกับ Server  
3. ไปที่ Network Settings → ตั้งเป็น  
> Obtain an IP address automatically  
4. เปิด Command Prompt → พิมพ์ 
```
ipconfig /renew
```
5. ตรวจสอบ IP ที่ได้รับ  
```
ipconfig /all
```
ตัวอย่างผลลัพธ์:
```
IPv4 Address : 192.168.1.101
DHCP Enabled : Yes
DHCP Server : 192.168.1.10
```

📸 *Client ได้รับ IP จาก DHCP Server*  
![Client IP Config](./screenshots/client-ipconfig.png)

---

### 🪜 ขั้นตอนที่ 5: สร้าง File Server และแชร์โฟลเดอร์
1. บน Server → สร้างโฟลเดอร์ `D:\Shared_Files`
2. คลิกขวา → Properties → Sharing → Advanced Sharing → ✅ Share this folder
3. ตั้งสิทธิ์ผู้ใช้:
- Everyone → Read / Write  
- หรือสร้าง User ใหม่ เช่น `staff01`
4. บน Client → เปิด File Explorer → พิมพ์  
5. ✅ ถ้าเข้าได้ แสดงว่า File Server ทำงานถูกต้อง

📸 *ตัวอย่างการแชร์โฟลเดอร์ในเครือข่าย*
![File Server Screenshot](./screenshots/file-sharing-test.png)
---

